name = "dice"
description = "Dice-rolling test agent — !roll 1d5 dispatches tool call via two-step pipeline"
background = false
allows_reload = true
persists_state = false

[[steps]]
id = "dice-detector"
type = "dice-detector"
trigger = "user_message"

[[steps]]
id = "dice-executor"
type = "tool-executor"
trigger = "step_complete:dice-detector"
